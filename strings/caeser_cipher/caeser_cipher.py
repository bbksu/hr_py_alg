def caesarCipher(s, k):
    # Write your code here
    print(list(map(ord, s)))
    if 
    # print(list(map(lambda x:x+k,map(ord, s))))

    # print("".join(map(chr, )))


if __name__ == '__main__':    

    # n = int(input().strip())

    # s = input()
    s = "middle-Outz"

    # k = int(input().strip())
    k = 2

    result = caesarCipher(s, k)
    result = caesarCipher("abcdefghijklmnopqrstuvwxyz", 3)
